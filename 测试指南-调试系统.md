# 调试系统测试指南

## ⚠️ 重要说明

之前日志只有一行的原因是 **Electron 主进程的注入脚本可能没有生效**。

现在我已经修改了代码,使用**多种方式确保日志被记录**:
1. ✅ 直接调用 `electronAPI.logDebug()` (最可靠)
2. ✅ 存储到 `window.__walkerDebugLogs` 对象
3. ✅ 存储到 `localStorage` (持久化)
4. ✅ 控制台输出 (console.log)

## 🧪 测试步骤

### 第1步: 重启应用

**重要**: 必须完全重启应用才能加载新代码!

```bash
# 1. 关闭 Electron 应用(关闭所有 Electron 窗口)

# 2. 重新启动
npm run dev
```

### 第2步: 验证调试系统是否工作

#### 方法A: 使用快捷键查看日志状态

1. 在 Electron 窗口中按 **`Ctrl+Shift+L`**
2. 打开开发者工具 (F12),查看 Console 标签
3. 你应该看到类似输出:
   ```
   === Current Debug Logs ===
   Characters: ['绿油油', '刘小红']
   
   [绿油油] 1 entries:
     [0.000s] Sprite image loaded
   
   [刘小红] 1 entries:
     [0.000s] Sprite image loaded
   === End Debug Logs ===
   ```

如果看到 `Characters: []`,说明调试系统**没有正常工作**!

#### 方法B: 使用快捷键导出日志文件

按 **`Ctrl+Shift+D`** 应该会自动下载一个日志文件

#### 方法C: 使用快捷键通过 ElectronAPI 导出

按 **`Ctrl+Shift+E`** 应该弹出对话框显示日志文件位置

### 第3步: 观察角色行走

1. 等待角色开始行走
2. 观察控制台输出(按 F12 打开开发者工具)
3. 你应该看到 `[DEBUG-绿油油]` 或 `[DEBUG-刘小红]` 开头的日志

### 第4步: 观察跳帧现象

当角色进入 END 阶段(接近停止)时,仔细观察:
- 看控制台是否有 `[DEBUG-角色名] END phase:` 的日志
- 是否有跳帧现象

### 第5步: 导出日志

#### 选项1: 使用 Ctrl+Shift+D (推荐)
- 会自动下载一个 `walker-debug-xxx.txt` 文件
- 将文件内容复制给我

#### 选项2: 使用 Ctrl+Shift+L
- 在控制台打印所有日志
- 复制控制台输出给我

#### 选项3: 查看 localStorage
1. 打开开发者工具 (F12)
2. 切换到 Console
3. 输入: `JSON.parse(localStorage.getItem('__walkerDebugLogs'))`
4. 复制输出给我

## 🔧 故障排除

### 问题1: 日志仍然只有一行

**可能原因**:
- Electron 没有完全重启
- 使用的是缓存的旧代码

**解决方法**:
```bash
# 1. 完全关闭所有 Electron 进程
taskkill /F /IM electron.exe

# 2. 清除缓存(如果是 vite 开发服务器)
npm run dev
# 在浏览器中按 Ctrl+Shift+R 强制刷新

# 3. 或者重新构建
npm run build
```

### 问题2: 快捷键没有反应

**检查**:
1. 焦点是否在 Electron 窗口上
2. 开发者工具是否抢占了快捷键

**解决方法**:
- 先关闭开发者工具
- 点击 Electron 窗口确保获得焦点
- 再按快捷键

### 问题3: 控制台没有 DEBUG 日志

**检查代码是否执行**:
1. 按 `Ctrl+Shift+L` 查看是否有日志
2. 如果有,说明调试系统工作正常,只是 console.log 被重定向了
3. 如果没有,说明 WalkerCharacter 组件没有加载

**临时解决方案**:
直接在控制台运行:
```javascript
console.log('Walker debug:', window.__walkerDebugLogs);
```

## 📊 如果一切正常,你应该看到

控制台中会有大量类似这样的日志:

```
[DEBUG-绿油油] [0.000s] Sprite image loaded
[DEBUG-绿油油] [5.234s] === Start walk === loopCountTarget=7, startMin=0
[DEBUG-绿油油] [7.891s] === Transition from LOOP to END === progress=0.4523, endMin=52
[DEBUG-绿油油] [7.891s] rAF tick: phase=end, elapsed=16.67ms, time=7891, frame=52
[DEBUG-绿油油] [7.891s] END phase: frame=52/82, elapsed=16.67ms, easeCoeff=1.000, delta=0.000417, progress=0.4527
[DEBUG-绿油油] [7.908s] rAF tick: phase=end, elapsed=16.67ms, time=7908, frame=53
[DEBUG-绿油油] [7.908s] END phase: frame=53/82, elapsed=16.67ms, easeCoeff=0.935, delta=0.000389, progress=0.4531
...
[DEBUG-绿油油] [8.241s] === END phase complete === final progress=0.4589, walkEndP=0.4523
```

**END 阶段应该有多条日志**,而不是只有一条!

## 🎯 下一步

一旦你确认调试系统工作正常:

1. 等待角色行走并进入 END 阶段
2. 按 `Ctrl+Shift+D` 导出日志
3. 将日志文件内容**完整复制给我**
4. 我会分析跳帧的根本原因

---

**准备好了吗?请完全重启应用,然后按 `Ctrl+Shift+L` 检查调试系统是否工作!**
