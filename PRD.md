# Lil-Agents-WinCC — 产品需求文档 (PRD)

## 项目概述

Lil-Agents-WinCC 是一款 Windows 桌面像素宠物应用。两个 AI 驱动的角色在任务栏上漫游，点击即可与 Claude 对话。

## 目标用户

- 喜欢个性化桌面的普通用户
- 希望在工作中有 AI 陪伴的开发者与创作者

## 核心功能

### 1. 桌面角色漫游

- 两个像素风角色（绿油油/刘小红）在 Windows 任务栏上自动行走
- 基于 Canvas rAF 的平滑动画，支持 START → LOOP → END 完整行走周期
- 角色自动规划路径，遇到屏幕边缘自动折返
- 点击穿透全屏覆盖层，角色悬浮于桌面之上，不阻挡正常操作

### 2. Claude AI 对话

- 点击角色弹出终端风格的聊天气泡
- 通过本地 `@anthropic-ai/claude-code` 子进程实时对话
- 支持 Markdown 渲染、代码高亮、链接跳转
- 每个角色拥有独立的会话上下文

### 3. 角色人格系统

- 每个角色通过独立的 `CLAUDE.md` 文件定义人格和对话风格
- 刘小红：热情活泼的打工人形象
- 绿油油：理性冷静的 AI 助手形象

### 4. 系统托盘

- 右键托盘图标打开菜单
- 支持切换角色可见性（绿油油/刘小红）
- 支持切换主题（霓虹/企业/毒性）
- 退出程序

### 5. 主题系统

- 三套 CSS 主题：霓虹（暗色青色）、企业（亮色蓝色）、毒性（矩阵绿色）
- 通过托盘菜单实时切换

### 6. 拖拽定位

- 长按拖拽角色到任意位置
- 拖拽时暂停动画，释放后恢复漫游
- 使用唯一拖拽 ID 机制，确保角色重叠时只有一个被拖动

### 7. 精确悬停检测

- 轮询 OS 光标位置（100ms 间隔）
- 使用 `document.elementFromPoint()` 确保只有最顶层角色响应
- 动态切换鼠标穿透状态，重叠时精确区分点击目标

### 8. 对话框零延迟跟随

- 对话框位置每帧通过直接 DOM 操作更新（`style.left/top`）
- 绕过 React 状态更新周期，实现与角色位置的实时同步
- 拖动角色时对话框完美跟随，无视觉跳动

### 9. 拖拽定位优化

- 长按拖拽角色到任意位置
- 拖拽时暂停动画，释放后恢复漫游
- 使用唯一拖拽 ID 机制，确保角色重叠时只有一个被拖动

## 技术栈

| 层级 | 技术 |
|---|---|
| 运行时 | Electron 28（主进程 + 渲染进程）|
| 前端 | React 18.2 + TypeScript 5.2 |
| 构建 | Vite 5 + `vite-plugin-electron` |
| 打包 | electron-builder（Windows）|
| AI | `@anthropic-ai/claude-code` CLI 子进程 |
