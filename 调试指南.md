# 跳帧问题调试指南

## 调试工具已就绪

我已经在代码中添加了完整的调试日志系统,可以详细分析 END 阶段(动画收尾阶段)的帧播放情况。

## 调试功能说明

### 1. 自动日志记录
- 所有调试日志会自动记录到浏览器的 `window.__walkerDebugLogs` 对象中
- 每个角色独立记录,便于分别分析
- 日志包含精确的时间戳(秒级精度)

### 2. 导出日志文件
**快捷键**: `Ctrl + Shift + D`

按下此组合键会自动:
1. 收集所有角色的调试日志
2. 生成一个文本文件 `walker-debug-{timestamp}.txt`
3. 自动下载到下载文件夹

### 3. 控制台日志
所有调试信息也会同步输出到浏览器控制台,带有 `[DEBUG-角色名]` 前缀

## 测试步骤

### 第一步:打开应用
1. 确保开发服务器已启动 (`npm run dev`)
2. 在浏览器中打开应用(通常是 http://localhost:5173)

### 第二步:打开开发者工具
1. 按 `F12` 打开开发者工具
2. 切换到 Console(控制台)标签页
3. 清空控制台历史(点击禁止图标或右键 Clear)

### 第三步:观察动画
1. 等待桌面宠物角色开始行走
2. 特别关注角色接近停止时的 END 阶段
3. 当你看到跳帧现象时,立即查看控制台

### 第四步:导出日志
1. 当你观察到跳帧后,立即按下 `Ctrl + Shift + D`
2. 这会下载一个包含完整调试日志的文本文件

### 第五步:分析日志
日志文件中会包含类似以下的信息:

```
=== 绿油油 ===
[0.000s] Sprite image loaded
[5.234s] === Start walk === loopCountTarget=7, startMin=0
[7.891s] === Transition from LOOP to END === progress=0.4523, endMin=52, time=7891
[7.891s] rAF tick: phase=end, elapsed=16.67ms, time=7891, frame=52
[7.891s] END phase: frame=52/82, elapsed=16.67ms, easeCoeff=1.000, delta=0.000417, progress=0.4527, time=7891
[7.908s] rAF tick: phase=end, elapsed=16.67ms, time=7908, frame=53
[7.908s] END phase: frame=53/82, elapsed=16.67ms, easeCoeff=0.935, delta=0.000389, progress=0.4531, time=7908
...
[8.241s] === END phase complete === final progress=0.4589, walkEndP=0.4523, time=8241
```

## 关键分析点

### 1. 时间间隔 (elapsed)
- **正常情况**: 应该稳定在 16.67ms (60fps) 或 33.33ms (30fps)
- **异常情况**: 出现突变,如从 16ms 跳到 50ms 或更高

### 2. 位置变化 (delta)
- **正常情况**: 应该平滑递减(因为 easeCoeff 在减小)
- **异常情况**: 突然的大幅跳跃,如从 0.0004 跳到 0.001

### 3. 帧时间 (time)
- **正常情况**: 连续递增,间隔均匀
- **异常情况**: 时间戳出现大的跳跃

### 4. rAF tick 与 END phase 的对应关系
对比 rAF tick 的 elapsed 和 END phase 的 elapsed,检查是否一致

## 常见问题模式

### 问题A: 帧时间不稳定
如果 elapsed 在 END 阶段出现波动(如 16ms -> 33ms -> 16ms),说明 rAF 调用间隔不均匀

### 问题B: 位置跳跃
如果 delta 值突然变大,可能是:
- easeCoeff 计算错误
- elapsed 突变导致位置计算异常
- 某个帧被跳过后累积到下一帧

### 问题C: 帧号跳跃
如果 frame 不是连续递增(如 52 -> 54,跳过了 53),说明某帧被跳过了

## 下一步

导出日志文件后,请将其提供给我,我会详细分析:
1. elapsed 时间的稳定性
2. delta 值的变化趋势
3. 是否存在帧跳过现象
4. 跳帧的根本原因

## 配置信息

根据 config.ts,当前角色的 END 阶段配置:
- **绿油油**: frame 52-82 (共31帧)
- **刘小红**: frame 44-80 (共37帧)
- 预期每帧持续时间: 50ms (FRAME_DURATION_MS)
- 使用二次缓出(ease-out)减速
