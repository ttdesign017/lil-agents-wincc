# 跳帧问题调试系统 - 使用说明

## 调试系统架构

我已经为你构建了一个完整的自动化调试系统,包含以下组件:

### 1. **数据收集层** (WalkerCharacter.tsx)
- ✅ 详细的帧级调试日志 (每次 rAF 调用)
- ✅ END 阶段专项分析 (位置、时间、缓动系数)
- ✅ 阶段转换追踪 (START → LOOP → END → IDLE)
- ✅ 全局日志存储 (`window.__walkerDebugLogs`)

### 2. **数据传输层** (Electron IPC)
- ✅ 渲染进程 → 主进程日志传输 (`logDebug` API)
- ✅ 文件写入系统 (自动写入用户数据目录)

### 3. **数据分析层** (自动分析)
- ✅ 帧时间稳定性检测
- ✅ 位置跳跃检测
- ✅ 帧号连续性验证
- ✅ 实时警告系统

## 自动化测试流程

### 方式一:自动日志文件 (推荐)

**优势**: 无需手动操作,日志自动保存到文件

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **正常使用应用**
   - 等待角色开始行走
   - 观察跳帧现象
   - 系统会自动记录所有调试信息

3. **查找日志文件**
   日志文件位置: `%APPDATA%/lil-agents-win/walker-debug.log`
   (通常是 `C:\Users\你的用户名\AppData\Roaming\lil-agents-win\walker-debug.log`)

4. **分析日志**
   将日志文件提供给我,我会进行详细分析

### 方式二:浏览器控制台导出

1. **打开开发者工具**
   - 在 Electron 窗口中按 `F12` 或 `Ctrl+Shift+I`

2. **运行分析脚本**
   - 打开 `debug-analyzer.js` 文件
   - 复制全部内容
   - 粘贴到浏览器控制台并执行

3. **查看实时分析**
   脚本会自动:
   - 监控调试日志
   - 检测异常情况
   - 在控制台输出警告

4. **生成完整报告**
   ```javascript
   analyzeWalkerLogs()
   ```

5. **导出日志文件**
   按 `Ctrl+Shift+D` 自动下载日志文件

### 方式三:开发者工具直接查看

1. **打开控制台** (`F12`)

2. **过滤调试日志**
   在控制台过滤框中输入: `[DEBUG-`

3. **实时观察**
   - 每次 rAF tick 都会输出日志
   - END 阶段会有详细信息
   - 异常时会有警告信息

## 日志格式说明

### rAF tick 日志
```
[0.123s] rAF tick: phase=end, elapsed=16.67ms, time=7891, frame=52
```
- `elapsed`: 距离上一帧的时间(ms)
- `time`: rAF 时间戳
- `frame`: 当前动画帧号

### END phase 日志
```
[0.123s] END phase: frame=52/82, elapsed=16.67ms, easeCoeff=1.000, delta=0.000417, progress=0.4527
```
- `frame`: 当前帧/最大帧
- `easeCoeff`: 缓动系数(1.0 → 0.0)
- `delta`: 位置变化量
- `progress`: 当前进度(0-1)

### 阶段转换日志
```
[0.123s] === Transition from LOOP to END === progress=0.4523, endMin=52
```
- 标记重要的阶段转换
- 记录转换时的精确状态

## 关键问题检测

### 🔴 高严重性

1. **帧时间不稳定**
   - 症状: `elapsed` 波动超过 10ms
   - 可能原因: 主线程阻塞、垃圾回收

2. **帧号跳跃**
   - 症状: 帧号不连续 (如 52 → 54)
   - 可能原因: rAF 回调执行延迟

### 🟡 中等严重性

1. **位置变化跳跃**
   - 症状: `delta` 突然增大或减小
   - 可能原因: 缓动计算错误、时间累积

## 配置文件

角色 END 阶段配置 (来自 `src/config.ts`):

| 角色 | 帧范围 | 总帧数 | 预期时长 |
|------|--------|--------|----------|
| 绿油油 | 52-82 | 31帧 | ~1550ms |
| 刘小红 | 44-80 | 37帧 | ~1850ms |

## 下一步分析计划

获得日志后,我会:

1. **提取 END 阶段完整数据**
   - 所有帧的 elapsed、delta、easeCoeff

2. **统计分析**
   - 平均帧时间、最大/最小值
   - 位置变化的平滑度
   - 是否存在帧跳过

3. **定位根本原因**
   - 时间系统问题
   - 缓动算法问题
   - rAF 调度问题
   - 状态同步问题

4. **提供修复方案**
   - 针对性修复代码
   - 原理解释

## 快速测试检查清单

- [ ] 应用已启动 (`npm run dev`)
- [ ] 角色可见并在行走
- [ ] 观察到跳帧现象
- [ ] 日志文件已生成 (`%APPDATA%/lil-agents-win/walker-debug.log`)
- [ ] 或将日志文件/控制台输出提供给我

## 故障排除

### 问题: 没有生成日志文件
**解决**: 检查控制台是否有 `[DEBUG] Log file initialized` 消息

### 问题: 日志为空
**解决**: 确保角色确实进入了 END 阶段(观察行走动画停止)

### 问题: 无法打开开发者工具
**解决**: Electron 默认启用,按 F12 或 Ctrl+Shift+I

---

**准备好了吗?请启动应用并等待角色行走,然后将日志文件提供给我进行分析!**
